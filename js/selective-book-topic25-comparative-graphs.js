// Topic 25: Selective Comparative Graphs & Data Displays.
(function(){
  const mr=window.SKILLUP_MR_EXTRA;if(!mr)return;
  const R=(a,b)=>Math.floor(Math.random()*(b-a+1))+a,shuffle=a=>[...a].sort(()=>Math.random()-.5),uniq=a=>[...new Set(a.map(String))];
  function Q(text,answer,choices,tip){const ans=String(answer);let rest=uniq(choices.map(String)).filter(x=>x!==ans);rest=shuffle(rest).slice(0,3);let k=1;while(rest.length<3){const v=String(k++);if(v!==ans&&!rest.includes(v))rest.push(v);}return{text,answer:ans,choices:shuffle([ans,...rest]),tip,topic:'sel_comparative_graphs',name:'Comparative Graphs & Data Displays'};}
  function argmax(a){let k=0;for(let i=1;i<a.length;i++)if(a[i]>a[k])k=i;return k;}
  function question(){const m=R(0,14);let a,b,d,k,ans,totalA,totalB;
    if(m===0){a=[R(10,20),R(10,20),R(10,20),R(10,20)];b=[R(10,20),R(10,20),R(10,20),R(10,20)];d=a.map((v,i)=>Math.abs(v-b[i]));k=argmax(d);ans=`Category ${k+1}`;return Q(`Two groups have values A: ${a.join(', ')} and B: ${b.join(', ')}. Where is the largest gap?`,ans,[ans,'Category 1','Category 2','Category 4'],'Compare absolute differences at matching positions.');}
    if(m===1){a=[R(4,10),R(4,10),R(4,10),R(4,10)];b=[R(4,10),R(4,10),R(4,10),R(4,10)];totalA=a.reduce((x,y)=>x+y,0);totalB=b.reduce((x,y)=>x+y,0);ans=totalA>totalB?'A':totalB>totalA?'B':'equal';return Q(`Across four categories, A = ${a.join(', ')} and B = ${b.join(', ')}. Which set has the greater total?`,ans,[ans,'A','B','equal'],'Add each row before comparing.');}
    if(m===2){a=[72,78,84,90];b=[80,82,84,86];return Q(`Two lines show A: ${a.join(', ')} and B: ${b.join(', ')}. At which period do they meet?`,'Period 3',['Period 1','Period 2','Period 3','Period 4'],'Find the matching pair with equal values.');}
    if(m===3){a=[68,74,81,89,94];b=[72,76,80,85,91];d=a.map((v,i)=>v-b[i]);k=argmax(d);return Q(`A = ${a.join(', ')}; B = ${b.join(', ')}. At which period is A furthest above B?`,`Period ${k+1}`,[`Period ${k+1}`,'Period 1','Period 3','Period 5'],'Subtract B from A at each period.');}
    if(m===4){a=[80,84,87,91];b=[75,82,88,94];const ca=a[3]-a[0],cb=b[3]-b[0];ans=ca>cb?'A':cb>ca?'B':'equal increase';return Q(`A changes ${a[0]}→${a[3]}; B changes ${b[0]}→${b[3]}. Which increases more overall?`,ans,[ans,'A','B','equal increase'],'Compare final minus initial.');}
    if(m===5){a=[12,15,19,24];d=[a[1]-a[0],a[2]-a[1],a[3]-a[2]];k=argmax(d);return Q(`One line has values ${a.join(', ')}. Between which periods is the steepest rise?`,`Period ${k+1} to ${k+2}`,[`Period ${k+1} to ${k+2}`,'Period 1 to 2','Period 2 to 3','Period 3 to 4'],'Compare consecutive increases.');}
    if(m===6){const known=[14,18,22],target=75;ans=target-known.reduce((x,y)=>x+y,0);return Q(`A four-category bar total is ${target}. Three values are ${known.join(', ')}. What must the fourth value be?`,ans,[ans,ans+5,ans-5,target],'Use the total to find the missing bar.');}
    if(m===7){a=[18,21,25,30];b=[20,23,27,32];return Q(`A = ${a.join(', ')}; B = ${b.join(', ')}. Which statement is true?`,'B is always 2 greater than A',['B is always 2 greater than A','A is always 2 greater than B','the gap increases each period','the sets are equal twice'],'Compare paired differences.');}
    if(m===8){return Q('You want to compare two schools’ enrolments across five year levels. Which display is most suitable?','double bar graph',['double bar graph','double line graph','circle graph','tree diagram'],'The x-axis contains categories, not a time trend.');}
    if(m===9){return Q('You want to compare two temperatures recorded each day for a week. Which display is most suitable?','double line graph',['double line graph','double bar graph','Venn diagram','histogram only'],'Daily values form an ordered time sequence.');}
    if(m===10){a=[10,14,18,22];b=[22,18,14,10];return Q(`A = ${a.join(', ')} and B = ${b.join(', ')}. What best describes the relationship?`,'A rises while B falls at the same rate',['A rises while B falls at the same rate','both rise','both fall','A is always greater'],'Look at the change from one position to the next.');}
    if(m===11){a=[15,18,21,24];b=[12,17,22,27];d=a.map((v,i)=>Math.abs(v-b[i]));k=argmax(d);return Q(`A = ${a.join(', ')}; B = ${b.join(', ')}. At which period is the absolute difference greatest?`,`Period ${k+1}`,[`Period ${k+1}`,'Period 1','Period 2','Period 3'],'Ignore which is higher; compare gap size.');}
    if(m===12){a=[9,12,15,18];b=[11,12,14,20];const equal=a.map((v,i)=>v===b[i]?i+1:null).filter(Boolean);ans=equal.length?equal.map(x=>'Period '+x).join(', '):'none';return Q(`A = ${a.join(', ')}; B = ${b.join(', ')}. When are the values equal?`,ans,[ans,'Period 1','Period 3','none'],'Compare matching positions.');}
    if(m===13){a=[25,30,28,35];b=[20,27,29,31];const net=a.reduce((s,v,i)=>s+v-b[i],0);return Q(`Across four categories, A = ${a.join(', ')} and B = ${b.join(', ')}. By how much does A exceed B in total?`,net,[net,Math.abs(net),net+4,net-4],'Add the category-by-category differences, or compare totals.');}
    return Q('What is the main purpose of a key on a double graph?','to show which visual series represents each data set',['to show which visual series represents each data set','to replace the scale','to calculate the mean','to sort values automatically'],'Without the key, the two series cannot be identified reliably.');
  }
  mr.topics=mr.topics.filter(x=>x[0]!=='sel_comparative_graphs');
  const i=mr.topics.findIndex(x=>x[0]==='sel_solid_views');
  mr.topics.splice(i>=0?i+1:mr.topics.length,0,['sel_comparative_graphs','▥','Comparative Graphs & Data Displays','Harder paired-data, trend, gap, total and missing-value reasoning']);
  const old=mr.question.bind(mr);mr.question=id=>id==='sel_comparative_graphs'?question():old(id);
})();